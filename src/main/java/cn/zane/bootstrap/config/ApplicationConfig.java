package cn.zane.bootstrap.config;

import org.springframework.context.annotation.Configuration;

/**
 * 应用全局配置。
 *
 * <p>承载与 Agent 框架无关的通用 Bean 定义，如全局对象映射器、
 * 任务调度器、跨切面关注点等。随 Phase 迭代逐步扩充。
 *
 * @author Zane
 */
@Configuration
public class ApplicationConfig {
    // Phase 2+ 逐步添加 Bean
}
